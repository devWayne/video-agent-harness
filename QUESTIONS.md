# Open Questions

按依赖顺序一次确认一个问题。

## Q1 — 项目术语（已解决）

确认是 **Video Agent Harness**：视频 Agent 的执行与编排层。

## Q2 — 目标模型（已解决）

长期目标包含 **Seedance 2.5** 与 **Wan 3.0**；首个可验收 Provider 锁定为已开放的 `wan2.7-t2v`。每个新模型必须先建立独立、经过官方契约验证的 Profile，不能只改模型字符串冒充兼容。

## Q3 — API 接入渠道（已解决）

MVP 仅通过用户自己的阿里云百炼官方账号直连 Wan。先用 Wan 2.7 跑通，Wan 3.0 授权生效后通过配置切换；火山方舟和第三方聚合通道均不进入首版。

## Q4 — MVP 交付边界（已解决）

输入一句创作 Brief 和可选参考素材，输出 5–60 秒工作流；当前真实 Profile 支持文本与一个可选音频参考，生成模型自动提供匹配音频。字幕、独立配音/BGM 和镜头级重做属于下一产品切片，不作为当前已完成能力宣称。

## Q5 — 画幅与交付分辨率（已解决）

默认 16:9 横屏，最终交付 UHD 4K（3840×2160）；模型原始产物与最终母版分层，允许 1080P 素材经过超分、修复与合成后交付。

## Q6 — Agent 运行时与主技术栈（已解决）

确认必须使用 TypeScript/Node.js。使用 `@earendil-works/pi-agent-core` 承担 Agent loop、工具调用、流式事件与人工 steering；使用独立的持久化工作流/任务状态机承担模型任务、重试、回调、超时、渲染和 4K 母版。

## Q7 — 百炼账号配置（已解决）

地域、Workspace ID、模型 ID 和 API Key 均在部署后台配置。Wan 2.7 Key 已完成真实烟测；OSS/IMS 使用另一套 RAM/STS 运行身份，真实云交付验收必须等待该身份完成。

## Q8 — 浏览器配置（申请处理中）

已在用户账号的华北 2（北京）百炼控制台确认默认业务空间和 `wan3.0-video`。Wan 3.0 当前仍为“申请中”，真实调用返回 `AccessDenied`；同一 Key 与 endpoint 切换 `wan2.7-t2v` 后已成功生成。明文只保存在被 Git 忽略的 `.env.local`。

## Q9 — 付费调用审批（已解决）

默认自动导演模式：脚本和分镜完成后直接生成，不要求人工确认。当前记录成本预估；后台硬上限和异常熔断属于部署前的下一运维切片，尚不宣称已实现。

## Q10 — 是否基于现有开源视频 Agent（已解决）

从零实现自有 Harness，不直接 fork。选择性参考 OpenReels、Tsugite、Code2MP4、ViMax、OpenMontage 和 video-use 的设计，并优先把 Pi Agent Core、HyperFrames 和云端媒体服务作为隔离后的底层依赖。

## Q11 — 4K 是否依赖 FFmpeg 超分？（已解决）

不依赖。4K 是独立云服务阶段，首个适配器使用阿里云 IMS SR5；FFmpeg 如后续引入，只做封装、混音、探测等确定性媒体处理。

## Q12 — 自动候选与选片（分阶段实现）

每镜头默认生成 2 个候选；当前纵向切片采用“首个成功候选”确定性策略。VLM 多维评分、低置信度补第 3 个和人工升级仍是明确的下一阶段能力。

## 后续问题（暂不展开）

- 首版字幕、独立旁白/BGM 和包装模板的具体产品形态。
- 中国内地生产发布涉及的数据驻留、内容审核和版权流程。
- 多实例部署时 PostgreSQL 与持久化队列的选型。
- 实际并发量、单任务硬成本上限与自动降级策略。
- 是否涉及真人肖像、品牌资产、版权音乐和商业发布。
