# Vision

## 目标

建立一个可扩展的视频生成 Agent Harness，将创意输入转化为可追踪、可重试、可评估的视频生产任务，并通过统一接口连接多个云端视频模型。

## 首版建议范围

- 输入：文本提示词、参考图片；后续扩展音频与视频参考。
- 生成：MVP 仅接入阿里云百炼 Wan 3.0；使用稳定业务别名映射实际 Model ID。火山方舟 Seedance 2.5 作为后续 Provider。
- 编排：提交、轮询、取消、重试、幂等、供应商切换与降级。
- 输出：默认 16:9 横屏；统一任务状态、逐镜头素材、生成参数、错误与成本元数据，并形成 3840×2160 的 4K 交付成片。
- 集成：上层 Agent 或业务工程通过 REST/OpenAPI 调用 Harness。

## 暂不默认纳入首版

- 面向终端用户的完整剪辑 UI。
- 自动发布到抖音、小红书等外部平台。
- 未经确认的真人肖像或版权素材处理。
- 自动触发付费模型调用。

## 成功标准（待确认）

1. Mock 模式下可从一句创作 Brief 和可选参考素材生成 15–60 秒、16:9 横屏的完整视频，覆盖分镜、画面、配音、字幕和基础配乐，并形成 3840×2160 交付版本。
2. 阿里云百炼 Wan 3.0 通过一次真实账号调用，并完整拿到持久化后的产物。
3. 同一业务请求可幂等提交，并能查询统一状态。
4. 云端临时产物能被及时转存到持久对象存储。
5. 单次任务有可观测的延迟、失败原因与预估/实际成本。
6. 任一镜头可独立重做，并保留其余已确认镜头。
